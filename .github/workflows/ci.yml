name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check formatting
        run: make format-check

      - name: Run Markdown linting
        run: make lint

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Check repo invariants (frontmatter, Exit Criteria, counts)
        run: make check-invariants

      - name: Check printed-bracket geometry against measured inputs
        run: make check-brackets

      - name: Run YAML linting
        uses: ibiqlik/action-yamllint@v3
        with:
          file_or_dir: .
          config_data: |
            extends: default
            ignore: |
              node_modules/
            rules:
              line-length: disable
              document-start: disable
              truthy: disable
